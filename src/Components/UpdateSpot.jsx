import { useLoaderData } from "react-router-dom";


const UpdateSpot = () => {
    const spots = useLoaderData()
    return (
        <div>
            
        </div>
    );
};

export default UpdateSpot;